export type row = any[];
export type rows = row[];
export type tableData = {headings: row, data: rows}
export type coordinate = {latitude: number, longitude: number, score: number}
export type coordinateList = coordinate[]